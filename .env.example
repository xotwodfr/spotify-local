# Copy to `.env.local` and set your Navidrome server URL.
# Used only server-side (BFF proxy) — never exposed to the client unless
# you also set NEXT_PUBLIC_NAVIDROME_URL for display on the login screen.
NAVIDROME_URL="http://localhost:4533"